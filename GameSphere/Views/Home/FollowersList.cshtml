@model List<User>
@{
    ViewData["Title"] = "Followers";
}

<h1>Followers</h1>
@if (Model.Count <= 0)
{
    <div>
        You don't have any followers
    </div>
}
@if (Model.Count > 0)
{
    @foreach (User u in Model)
    {
        <div>
            @u.UserName
        </div>
    }
}