@model List<User>
@{
    ViewData["Title"] = "Following";
}

<h1>Following</h1>
@if (Model.Count <= 0)
{
    <div>
        You aren't following anyone
    </div>
}
@if (Model.Count > 0)
{
    @foreach (User u in Model)
    {
        <div>
            @u.UserName
        </div>
    }
}